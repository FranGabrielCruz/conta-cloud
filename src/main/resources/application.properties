spring.application.name=ContaCloud
server.port=${SERVER_PORT:8080}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/conta_cloud}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:FRANpostgres1990}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

spring.threads.virtual.enabled=true
spring.web.error.include-message=never
spring.web.error.include-stacktrace=never
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

vaadin.launch-browser=${VAADIN_LAUNCH_BROWSER:false}
logging.level.org.springframework.security=INFO

# Bootstrap opcional para una instalacion nueva. No se ejecuta si faltan valores.
contacloud.bootstrap.empresa-codigo=${BOOTSTRAP_EMPRESA_CODIGO:001}
contacloud.bootstrap.empresa-nombre=${BOOTSTRAP_EMPRESA_NOMBRE:DEMO}
contacloud.bootstrap.usuario=${BOOTSTRAP_USUARIO:admin}
contacloud.bootstrap.password=${BOOTSTRAP_PASSWORD:ContaCloud2026!}
